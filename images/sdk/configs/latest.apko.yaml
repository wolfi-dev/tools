contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - https://packages.wolfi.dev/os
  packages:
    - ca-certificates-bundle
    - wolfi-baselayout
    - sdk
    - grype
entrypoint:
  command: /usr/bin/sdk

archs:
  - x86_64
  - aarch64
